# Proxmox VE Configuration
# Required for 'homelab proxmox' commands

# ==========================================
# MIGRATION GUIDE (BREAKING CHANGE)
# ==========================================
# The environment variables have been refactored for better clarity and usability.
#
# OLD FORMAT (no longer supported):
#   PROXMOX_API_TOKEN=root@pam!homelabcli=bd2ed89e-6a09-48e8-8a6e-38da9128c8ce
#   PROXMOX_HOST=https://proxmox.home.sflab.io:8006
#
# NEW FORMAT (use these):
#   Split the API token into components:
#   - user@realm!tokenid=secret becomes:
#     PROXMOX_USER=user
#     PROXMOX_REALM=realm
#     PROXMOX_TOKEN_KEY=tokenid
#     PROXMOX_TOKEN_SECRET=secret
#
#   Split the host URL into components:
#   - https://hostname:port becomes:
#     PROXMOX_HOST=hostname (without protocol)
#     PROXMOX_PORT=port (defaults to 8006 if omitted)
# ==========================================

# Proxmox user (e.g., root, admin)
PROXMOX_USER=root

# Authentication realm (e.g., pam, pve)
PROXMOX_REALM=pam

# Token identifier/name (e.g., homelabcli)
PROXMOX_TOKEN_KEY=homelabcli

# Token secret (UUID format)
# Generate via: Datacenter > Permissions > API Tokens in Proxmox UI
PROXMOX_TOKEN_SECRET=

# Proxmox hostname (without protocol or port)
# Example: proxmox.home.sflab.io or 192.168.1.100
PROXMOX_HOST=proxmox.home.sflab.io

# Proxmox port (optional, defaults to 8006)
PROXMOX_PORT=8006
